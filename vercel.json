{
  "buildCommand": "cd vega-app && npm install && npm run build",
  "outputDirectory": "vega-app/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
